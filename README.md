# Maven Status
*Morningside Design LLC — automated build and deployment status*
Last updated by Claude Code.
2026-06-16 09:02:55 UTC — Status board live — gap-fix session in progress
2026-06-16 09:05:54 UTC — Gap fixes complete — hash chain, status board, skills, M016 M017 added
2026-06-16 10:22:00 UTC — Migration renamed M016→M018 audit_log hash chain
2026-06-16 10:41:14 UTC — Morning check — reading Phase 2 report
2026-06-16 11:10:28 UTC — Morning setup complete — family email set, M018 migrated, pipeline worker live with Gemini Flash
2026-06-16 12:05:08 UTC — SDK fix deployed — Gemini Flash extraction live
2026-06-16 13:00:21 UTC — End-to-end test BLOCKED — pipeline crashes on real Postgres (dict(row) on tuple at pipeline.py:410; codebase row-factory mismatch). Worker scaled to 0. Needs HIGH-blast-radius db.py/de-id fix + review. Diagnosis ready for Chris.
2026-06-16 13:28:54 UTC — Session start — Maven row factory fix. Running Gate 1 diagnostic (read-only).
2026-06-16 13:31:14 UTC — maven-rowfix: confirmed in correct tmux session. Gate 1 diagnostic starting (read-only). 9 concurrent sessions on shared checkout — will verify branch/stash before any write.
2026-06-16 13:38:05 UTC — maven-rowfix HALTED at Gate 2: M019 row-factory fix is ALREADY in progress in another concurrent session (uncommitted WIP on db.py/retrieve.py/deidentify.py/dreaming.py + specs/M019_db_row_factory.md, files edited 09:30-09:33 during my diagnostic). Prescription was stale — db.py already has dict_row. Not proceeding to avoid clobbering live work. Escalating to Chris.
2026-06-16 13:47:55 UTC — maven-rowfix session complete — stood down on M019 (owned by concurrent session). The retrieve.py:223 gap I flagged was independently fixed by that session mid-run; recorded the resolution in DIAGNOSTIC_REPORT_row_factory.md. No code edits, no spec, no commit from this session. Repo untouched.
2026-06-16 13:52:57 UTC — M019 row-factory fix DEPLOYED & verified in prod — worker now claims jobs, runs de-id, reaches Gemini (no crash). End-to-end extraction blocked ONE step further by an INVALID Gemini API key in maven/gemini secret (needs Chris). Worker live & stable.
2026-06-16 13:53:02 UTC — Maven website live at GitHub Pages — privacy policy and terms ready
2026-06-16 14:35:43 UTC — End-to-end: Gemini key injection FIXED (extract 200 OK, candidates returned). Now blocked at candidate write — events table has no 'detail' column the pipeline INSERTs (schema/code drift vs §6). Worker stable. Needs a decision: add events.detail migration vs map to existing cols.
2026-06-16 15:11:22 UTC — iOS wiring session start — pointing app at live App Runner API, rebranding YodaClaw→Maven (ios/ only, parallel-safe)
2026-06-16 15:15:44 UTC — M020 applied. End-to-end found a 3rd schema/code drift: entity_map missing child_id (re-id of child_token). M021 written + verified on ephemeral pg (process_job PROCESS_OK). Security review in progress; will apply + complete end-to-end on APPROVE.
2026-06-16 15:19:25 UTC — iOS wired to live backend — API URL updated, app renamed Maven (bundle com.morningsidedesign.maven, /auth/apple, API-driven child colors). BUILD SUCCEEDED, pushed to main 8c3fa88
2026-06-16 15:25:30 UTC — END-TO-END EXTRACTION COMPLETE ✅ — test email → Gemini Flash → candidate events row (title='practice', conf 0.9, band review, status proposed) for family 11111111. Fixes: M019 row-factory, Gemini key-injection (api_key JSON field), M020 events.detail, M021 entity_map.child_id. Worker live & stable.
2026-06-16 15:41:41 UTC — Schema-vs-code audit complete: 7 drifts, only 1 (gmail connect audit_log.actor_member_id) breaks real Gmail connect — must fix before connecting. Other 6 latent/feature-tied. Report: docs/DIAGNOSTIC_REPORT_schema_audit.md
2026-06-16 16:06:49 UTC — M022 deployed ✅ — Gmail-connect audit_log bug fixed (actor_member_id→actor + json.dumps detail). Verified end-to-end on ephemeral pg + suite 281 passed + security review APPROVE. App Runner RUNNING, /health 200. Gmail connect unblocked.
2026-06-16 17:51:53 UTC — Diagnostic: /auth/apple 401-not-404 root cause = lookup_member_by_apple_sub() is a NotImplementedError stub (never wired to the SQL function). SQL function healthy in RDS. Report: docs/DIAGNOSTIC_REPORT_auth_apple_404.md
2026-06-16 18:11:08 UTC — /auth/apple fix scope expanded: API has NO DB access (would still 401) + app role 'maven' BYPASSES RLS (isolation not enforced — P0). Recommend provisioning maven_app NOBYPASSRLS role. Decision needed before deploy.
2026-06-16 18:50:04 UTC — M024: maven_app role + RLS-enforcement + secret + IAM + code all DONE & verified. Last blocker: App Runner (public egress) can't reach PRIVATE RDS — needs a VPC connector + NAT gateway (infra+cost). /auth/apple still 401 until then. Decision needed.
2026-06-16 18:57:46 UTC — M024 DEPLOYED ✅ — /auth/apple resolver wired; API runs as maven_app (NOBYPASSRLS) so RLS is now ENFORCED for the API; maven/db-app secret + App Runner instance role; RDS temporarily public (MVP, documented, M026 to revert). API RUNNING /health 200. Ready for real Apple sign-in test (unknown sub -> 404 -> hatch).
2026-06-16 19:54:52 UTC — M027 backend hatch PR #18 open + verified (ephemeral-pg as maven_app, full suite 306 pass, security-review APPROVE). codex-review red = false positives/soft advisories (disposition posted). Awaiting Chris merge. iOS=M028.
2026-06-16 20:07:37 UTC — M027 PROD DEPLOY complete: image 832dacd5 pushed; M014_hatching.sql applied to RDS (exit 0, 3 tables RLS-forced, maven_app auto-granted, no-GUC reads clean 0); App Runner auto-deployed (RUNNING); /health 200; POST /families live (401 fail-closed). Hatch endpoints are live in prod.
2026-06-16 20:30:02 UTC — M028 iOS hatch UI wired into RootView — PR #19 open. HatchView ported onto main (M007), Bearer-auth adapted to live endpoints, Apple-token threaded. YodaApp BUILD SUCCEEDED; HatchTests 14/14 + Spec007 16/16. codex red = false positives (disposition posted). YodaWatch WidgetStore failure pre-existing. Awaiting Chris merge.
2026-06-16 20:50:46 UTC — Hatch 403 root-caused: NOT a token-type bug (client correctly sends Apple token as Bearer; server verifies it). It was the M015 admission gate denying — Apple omits email claim on repeat sign-ins so the CHRIS_FAMILY_EMAIL allowlist failed with 403 onboarding_not_open. Chris-approved fix: set COUNSEL_REVIEW_COMPLETE=true on App Runner (gate open for test-family-zero). Redeployed, RUNNING, /health 200, /families now admits verified sign-ins. Documented as temp exception; revert before beta.
2026-06-16 21:37:34 UTC — Hatch /children 500 fixed + deployed. Root cause: the API never wired the production de-id backend (PostgresEntityMap) — only the worker did — so create_child's entity_map token allocation hit the fail-closed NullEntityMap (NotImplementedError). Fix: wire PostgresEntityMap in create_app() guarded on DB config. Verified (suite 306 pass; ephemeral allocate as maven_app; entity_map grants on RDS; DB_SECRET injected). Deployed image 3db14be, App Runner RUNNING, /health 200. PR #20.
2026-06-16 22:11:29 UTC — Both tasks done. TASK1: deleted Chris's partial family (8e168e9b) transactionally in FK order, verified gone + no members row → he can re-hatch. TASK2: M030 resumable hatching — GET /hatch/status + iOS resume routing + HatchView startStep (never duplicates family) + empty-week resume prompt. Backend 312 pass, iOS HatchTests 18 + Spec007 16, ephemeral /hatch/status as maven_app correct+isolated, security review APPROVE. PR #21 (stacked on #19). Not merged, not deployed.
2026-06-16 22:49:13 UTC — Hatch Gmail-connect fix (M031, PR #22): the connect step wasn't missing — connectEmail() was a STUB that discarded the OAuth URL and faked emailConnected=true. Fixed: startGmailConnect() fetches Google's consent_url (GET /auth/gmail/start) and the view opens it; only marks connected after the user authorizes. Build SUCCEEDED, HatchTests 14/14 + Spec007 16/16. FLAGGED 2 backend e2e blockers: Gmail OAuth creds PENDING + callback @require_capability(feed) can't auth a browser redirect. Stacked on #19, not merged/deployed.
2026-06-16 23:11:09 UTC — Both Gmail blockers done under full discipline (PRs #23 M032, #24 M033 — not merged/deployed). M032: callback auths via the signed OAuth state (no Bearer) + fail-closed state key; security review APPROVE; suite 309. M033: VERIFIED diagnosis — the stored client_secret is a copy of the client_id (real secret missing) + App Runner wired nothing; provisioning runbook + docs + DR; consent URL verified locally with the real client_id; security review APPROVE. Irreducible Chris steps flagged: real client_secret + redirect registration + review.
2026-06-16 23:28:29 UTC — M034 UI fixes PR open
2026-06-16 23:42:47 UTC — M034 (UI) MERGED to main. Did NOT run the backend redeploy: (1) M034 is iOS-only — the Docker image is backend-only, so it carries zero UI changes; the Maven UI ships to devices via Xcode/TestFlight. (2) The live image (3db14be) is the M029 de-id fix, which lives only on the m029 branch — PR #20 is NOT merged, so rebuilding from main = M027-only = REGRESSES the hatch /children 500. Recommend: merge PR #20 (M029) before any backend redeploy. Prod left untouched (live image still serving).
2026-06-16 23:59:35 UTC — Merged #20→#21→#22→#23 into main in order (resolved KidsWeekView + 3 service-file conflicts with M034). Main clean: backend suite 315 passed, iOS BUILD SUCCEEDED. Rebuilt + deployed backend image 0174019 (sha 5a3308b8) — App Runner RUNNING, /health 200. Verified live: /hatch/status 401 (M030), /auth/gmail/callback 400 fail-closed (M032), /children 401 not 500 (M029 de-id intact). No regression. Note: gmail connect still needs M033/#24 (state key) to function; it fail-closes safely until then. M034 UI is iOS — reaches devices via Xcode/TestFlight, not this image.
2026-06-17 14:34:55 UTC — Home-screen 404 fixed: /api/morning-briefing is not served by the Maven backend (legacy YodaClaw aggregate). iOS now shows an empty state instead of a 404 (Home + briefing detail). BUILD SUCCEEDED, pushed 3746c03. /hatch/status: endpoint implemented; could not read Chris's live value from this env (yoda-deploy IAM has no Secrets Manager/RDS/App Runner access; prod rejects dev-key tokens).
2026-06-17 15:14:44 UTC — Gmail connect LIVE. Fixed the provision script's App Runner wiring bug (heredoc overrode the describe-service pipe -> empty JSON); completed the wiring live. App Runner RUNNING, all 4 GMAIL_OAUTH_* secrets injected, /health 200. Verified GET /auth/gmail/start returns a REAL Google consent URL (real client_id, gmail.readonly, signed state = state key wired). PR #24 (M033) merged + script fix on main. ⚠️ CRITICAL finding: MAVEN_SESSION_SIGNING_KEY is NOT injected into App Runner -> the live API signs/verifies session JWTs with the PUBLIC dev fallback key (forgeable -> full multi-tenant bypass). Flagged for immediate fix.
2026-06-17 15:24:42 UTC — CRITICAL session-key vuln FIXED + verified. Injected MAVEN_SESSION_SIGNING_KEY from maven/jwt + MAVEN_ENV=production into App Runner (added maven-jwt-read instance-role grant; preserved DB_SECRET + GMAIL_OAUTH_*). Redeployed, RUNNING, /health 200. Verified: dev-fallback-key token -> 401 (forgery closed), real maven/jwt token -> 200 (auth intact). All sessions invalidated (re-sign-in). Documented in AWS_RESOURCES.md.
